{
  "name": "boxexplore",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "wasm": "cd rust/boxcore && cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir ../../src/wasm target/wasm32-unknown-unknown/release/boxcore.wasm"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.4.3"
  }
}
